# Harmless AGENT runtime fixture

This fixture is deterministic and offline. It reads its bundled `request.json`, writes one JSON object to standard output, and exits. It does not open sockets, read environment variables, invoke tools, install packages, or modify files.
