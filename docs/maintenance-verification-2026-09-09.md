# 维护核实记录（2026-09-09）

本记录说明通用 Security Skill、扫描完成自动触发、单项目代码/业务图谱，以及认证失败和平台健康诊断的实现与实际验证。仓库、数据库和运行结果优先于历史文档。

## 开始状态

- 图谱实施开始时 `main` 工作区干净，`HEAD` 与 `origin/main` 同为 `4e73278394b2984fbd8cae1b5393a0af2e2ff8c3`，领先/落后 `0/0`。
- 六份维护基线文档已重新读取。数据库迁移起点为 `20260908_0017 (head)`，`security_skills` 与 `security_skill_runs` 均为 0。
- 当前验收项目只使用 `D:\project\PYproject\testproject`；没有使用 `test_project` 或 DVNA。

## 实现

- 数据库迁移 `20260909_0018` 增加内置标记、SCA/SAST/AGENT 自动触发选择、运行触发来源和触发扫描任务 ID，并用唯一索引保证同一 Skill/扫描任务只自动执行一次。
- 5 个内置 Skill 是租户级资产，对该租户所有项目通用，不包含测试项目文件、路由、变量、行号或目录条件。
- 管理员可在“安全知识中枢 → 规则与 Skill”逐项选择自动触发模块；普通用户可查看触发方式，但不能修改。
- SCA、SAST（含本地 Worker 实际调用的扫描路径）和 AGENT 只在扫描状态成功提交为 `completed` 后调用 Skill。自动复核失败写入独立状态，不会把已经完成的专业扫描改写为失败。
- 项目删除会先清理该项目的 Skill 运行记录，再删除扫描任务；租户级 Skill 定义本身保留，避免自动运行历史通过外键阻塞项目清理。
- Skill 仍只复核当前 Finding，不重新启动、替换或改变专业扫描器，也不执行任意上传代码。
- 迁移 `20260909_0019` 新增项目图谱快照表，以项目、图类型和版本唯一地保存节点、边、摘要、生成器版本、源指纹和局限。
- 代码图谱只读取项目源码，使用 Python AST 和 JavaScript/TypeScript 保守解析，跳过符号链接及常见生成目录，并设置文件、字节、节点和边上限；不执行受测代码。
- 业务图谱聚合最新代码路由、当前 Finding、DAST 业务流、角色、验证证据和已发布知识；精确关系与同文件/同规则启发式关联分开标注置信度和依据。
- 前端在“安全知识中枢 → 规则与 Skill”展示两类图谱并提供手动重建。这不改变扫描逻辑或用户/管理员边界。

## 真实验证

- 首次迁移尝试失败：Alembic `op.execute()` 把确定性 UUID 字符串中的冒号解释为绑定参数。事务完整回滚，数据库仍为 `0017` 且没有种子数据；改用驱动层原生 SQL 后升级成功。
- Skill 自动触发交付时，数据库与仓库迁移头均为 `20260909_0018`，当时实际执行 `0018 → 0017 → 0018` 成功。当前已进一步升级为 `20260909_0019 (head)`，仍有 5 个内置已发布 Skill 和 5 个 v1 版本。
- 使用 `testproject` 最新已完成 SAST 扫描事件验证自动触发：3 个适用 Skill 首次生成 3 条完成记录；同一事件第二次执行生成 0 条，幂等成立。验收运行、对应审计以及临时写入的扫描元数据随后全部恢复/清理。
- 管理员将 SCA Skill 从 `sca` 自动触发改为仅手动，再恢复为 `sca` 成功；两条测试审计已清理。
- 当前 42 条有效 Finding 上的只读匹配：跨模块高危复核 22 条、SAST 有证据复核 23 条、SCA 高危复核 8 条、AGENT 权限边界复核 0 条、整改跟踪复核 0 条。这些是选择器命中，不是新增漏洞、召回率或扫描成功率。
- 图谱迁移实际执行 `0019 → 0018 → 0019`；首次回退时索引删除语句失败，事务回滚保持数据库在 `0019`，修正索引删除语句后完整往返成功。降级按设计删除图谱表，随后通过实际 API 重建验收项目的 v1 快照。
- `testproject` 代码图谱 v1 实际读取 4 个源文件、12064 字节，0 个文件跳过，未截断；生成 110 个节点、129 条边和 14 个路由。
- `testproject` 业务图谱 v1 生成 68 个节点和 87 条边，包含 42 个当前 Finding、4 个业务流、5 个角色和 2 个证据节点。当前没有可适用的显式验证或已发布知识节点，未伪造关联。
- 数据库当前有用户 `use1` 手动执行产生的 1 条 Skill 记录；它不是本轮临时数据，已保留。

## 测试与界面

- Skill 定向后端历史结果为 `6 passed`；本轮图谱与项目删除顺序定向为 `12 passed`。首次从仓库根目录执行时因未带 API 包路径而收集失败，改从 `apps/api` 按项目约定重跑后通过；该首次失败不是代码用例失败。
- 完整后端：`407 passed, 1 skipped, 32 warnings`；唯一跳过是当前 Windows 账号不能创建符号链接，告警为既有 `datetime.utcnow()` 弃用提示。
- `npm run build` 通过；最大 JavaScript 包 `655.32 kB`，Vite 仍有大于 500 kB 的分包告警。
- `npm run test:skill-ui` 在 1440px 和 390px 通过，断言 5 个内置 Skill、15 个自动触发复选框、通用范围和安全边界，无横向溢出。
- `npm run test:graphs-ui` 在一次性 `AUTH_DISABLED=true` 隔离服务上以 1440px 和 390px 通过，脚本只读已有快照且无横向溢出；临时 8001/5174 服务已停止。`test:dev-server` 通过，常驻 `http://127.0.0.1:5173` 实测 HTTP 200。
- 既有 AGENT 综合冒烟仍因 `testproject` 没有 AGENT 扫描基线而失败；P0 不应因此标记通过。

## 剩余边界

- 尚无外部 Skill 包来源、签名/信任校验、安装卸载、定时或其他事件触发、独立并发队列、知识候选自动转换和效果基准。
- 未重新执行目标 SCA/SAST/AGENT 扫描；存量扫描状态不能作为本轮新扫描成功证据。
- 精确率、误报率、召回率、DAST 复现率、生态兼容率和生产就绪度没有新增基准。

## PPT 最终平台差距核对

- 2026-09-09 按项目所有者要求，将本机 `artifacts/audit-ppt-20260809/01.pptx` 作为最终平台目标蓝图逐页核对。该文件共 5 页，SHA-256 为 `7CE2B46AC1FDC66967129DB12E07417C6B26BF4F615F8EB0F0990C0FB26F4738`；`D:\project\PYproject\dast-ppt-review-20260817\source.pptx` 与其哈希相同。
- 核对以提交 `0247fef7a3884fec587c848db5558f5e835a117c`、数据库迁移 `20260909_0019`、当前服务和 `testproject` 数据为事实基线，没有把 PPT 的预期效果写成当前能力。
- 全部目标、当前状态、剩余缺口、验收标准和完成优先级集中写入 [`platform-target-gap-register.md`](platform-target-gap-register.md)。`deferred-work.md` 继续记录实施状态和完成证据。
- 当前 `testproject` 不包含 AGENT 内容。项目所有者决定暂时忽略因此产生的 AGENT 综合冒烟失败；机器可读 P0 状态在验收口径另行调整前仍保持失败，不能写成已经通过。
- 上述 PPT 差距文档核对步骤只修改文档和机器可读文档证据，当时没有修改扫描器、权限或数据库，也没有重新执行目标扫描或完整后端测试。`acceptance_check.py --profile baseline` 通过；P0 按既有口径仍因前端综合项失败。前端生产构建通过，最大 JavaScript 包当时为 655.32 kB 并保留 Vite 大包告警；`test:dev-server` 通过。Skill 冒烟首次连接日常认证服务时因未提供测试凭据而明确失败，随后在一次性 `AUTH_DISABLED=true` 的 8001/5174 隔离服务上与图谱冒烟一同通过 1440px/390px 验证，临时服务已停止。没有运行已决定暂时忽略的 AGENT 综合冒烟。

## 认证失败与健康诊断维护

- 根因核实：认证状态请求失败后只结束 loading，没有写入可渲染错误状态，页面可永久停留“正在连接”；`/auth/me` 把 401 与 HTTP 5xx、网络错误、超时全部吞成未登录；React Strict Mode 的重复初始化缺少旧请求隔离。旧 `/api/health` 又固定返回 `ok`，无法辅助判断数据库或工具依赖。
- 实现：只有 `/auth/me` HTTP 401 表示未登录；HTTP 错误、API 不可达、8 秒超时和数据库不可用显示真实诊断及重试，最新请求代次阻止旧结果覆盖。健康接口执行 PostgreSQL `SELECT 1`、Redis PING、Docker Engine、固定 Syft/Grype/Trivy 镜像及 Semgrep 就绪检查；必需依赖失败返回 503，可选依赖失败返回 `degraded`。认证页使用不探测本机工具的快速参数，避免工具检查拖延登录错误展示。
- 通用边界：实现不包含 `testproject` 文件、路由、变量、行号或目录条件，没有改变扫描器、权限或扫描状态。工具/镜像就绪明确写为“未执行扫描”；外部网络、情报时效、模型和目标状态继续由对应模块核实。
- 定向与界面：后端认证/健康定向 `13 passed, 2 warnings`；`test:auth-failure-ui` 的状态 503（1440px、390px）、身份 503、API 不可达、超时和重试全部通过；`test:dev-server` 及 `http://127.0.0.1:5173` HTTP 200 冒烟通过；生产构建通过，最大 JavaScript 包 `658.32 kB`（gzip `189.35 kB`），Vite 大包告警仍存在。
- 完整回归：D 盘独立临时目录下为 `414 passed, 1 skipped, 34 warnings`，无失败。唯一跳过仍是当前 Windows 账号不允许创建符号链接；告警为 32 条既有 `datetime.utcnow()` 和 2 条 FastAPI `on_event` 弃用提示。
- 实际运行：常驻 `/api/health` 返回 HTTP 200/`ok`，PostgreSQL、Redis、Docker、3/3 固定 SCA 镜像和 Semgrep 均就绪，但这不是扫描成功证据。数据库仍为 `20260909_0019`；`testproject` 对应项目 `test01`，只有 5 个 SAST 和 1 个 SCA 历史任务、没有 AGENT 任务，代码/业务图谱 v1 均为 `completed`。本轮未执行目标扫描、未创建账号或会话，也未使用 DVNA 或 `test_project`；完整测试专用临时目录已清理。
