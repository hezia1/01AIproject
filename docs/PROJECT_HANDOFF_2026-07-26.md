# 项目交接快照

> 文件名因历史链接保留；正文已更新到 **2026-09-04**，不再代表 2026-07-26 的实现状态。

2026-09-04 补充：管理中心按模块整理已有配置并修复长文本布局；SAST 默认政策/抑制、SCA VEX 与通用模块配置写入受管理员权限保护，模块启停保留配置。后续本轮增加每页 10 条数据列表、SCA/SAST 规则编辑、SANDBOX 镜像白名单与五类联网资源策略，数据库迁移到 `20260904_0016`。普通用户继续独立确认和执行 DAST；资源更新受管理员允许。命令/资源上限、DAST 公共映射及 Skill 安装系统仍未配置化，见 `docs/admin-configuration.md` 和 deferred-work 的 CFG-001 / UI-007 / UI-008。

## 当前结论

六模块的研发与演示闭环已经存在：SCA、SAST、AGENT 可执行静态扫描；DAST 可从受支持的 SAST/AGENT Finding 生成有界验证；SANDBOX 可在 Docker 中启动一次性目标并采集固定探针证据；ASPM 可在单项目范围汇总、关联、整改、复测和出报告。

导师现场提供陌生项目时，可通过本地目录、受控 ZIP 或 HTTP(S) Git 接入；系统自动给出静态/动态模块准备度。快速演示模式通过依赖文件、组件、源码文件、体积和软时限形成可解释的有界结果，但不会覆盖用户明确勾选的 SCA Docker 或已启用的 Semgrep；增强引擎会真实执行并保存成功、降级或失败证据，深度扫描仍应提前准备。

当前不是生产级企业平台。能力声明与量化验收基线已作为 P0 完成；知识中枢已交付候选、审核、版本/回滚和同租户跨项目推荐；AGENT、SCA 与 SAST 治理前端已完成任务型工作区拆分、中文化、双视口自动化冒烟和真实用户可用性收敛。最重要的未完成项是生产 IAM/角色权限/真实审批身份、可靠分布式任务、部署与敏感证据保护、跨项目风险治理，以及检测效果与动态复现的版本化基准语料。

2026-09-03 已增加适合当前单机阶段的本地 `user` / `admin` 两角色登录：首次管理员由页面初始化且没有默认密码，会话使用 HttpOnly Cookie，管理员写入接口由后端拒绝普通用户。一级导航收敛为“项目、检测、风险治理、安全知识中枢、报告”，管理员额外看到“管理中心”。普通用户继续拥有项目接入、五模块基础执行、ASPM、知识中枢、Grype 数据库更新和 Semgrep 社区规则更新能力；本地/项目规则写入与知识审核归管理员。该交付不等于生产 IAM，用户间项目隔离、外部身份源、MFA、密码恢复和生产会话治理仍未完成。

## 已验证状态

- 分支：`main`，远端：`origin`。
- API、Web、PostgreSQL、Redis 可在本地启动；API 健康检查正常。
- Alembic 数据库迁移已到 `20260903_0015 (head)`。
- 后端测试在 `D:` 盘临时目录下为 **317 passed, 1 skipped**；使用默认 `C:` 盘临时目录会因 Agent 暂存安全规则出现预期失败。
- 前端生产构建通过；Vite 仍有主包体积分包告警；仓库内 AGENT、SCA/SAST 与 SANDBOX 界面冒烟覆盖 1600px/390px 无横向溢出，SANDBOX 额外验证端口可编辑及非法值提示。SAST 扫描策略默认展示 Semgrep 增强入口并即时保存开关；SCA Docker 开关在快速/深度模式都会生效，工具实际状态随批次保存。
- SCA Docker 增强已按职责拆分：Syft 生成一次 SBOM，Grype 只读取 SBOM 做漏洞匹配，Trivy 默认只做配置与密钥检查；Grype 健康失败时 Trivy 才启用漏洞回退。过期 Grype 数据库不再在每次扫描中重复导入，同批次的配置/密钥结果独立于组件 CVE 数量保存，密钥原值不落库。
- SCA“扫描引擎与漏洞情报源”已增加 Grype 数据库检测与人工更新入口：显示 schema、构建时间和按 720 小时策略计算的预计失效时间；只有用户点击更新时才联网，下载后会再次校验，并与扫描共用 `artifacts/sca-offline/grype-cache`。
- SCA 快速与深度扫描均采用在线 OSV 优先策略；首个查询失败后整批回退人工准备的本地 OSV 镜像，在线结果不会自动写入镜像。显式离线 CI 仍保证不联网。
- SAST 社区规则更新已与扫描解耦：用户在治理界面确认 Semgrep Rules License 后才能从官方仓库显式下载，平台固定解析后的提交 SHA 并只保存安全 YAML；扫描不联网，按项目语言/配置类型挂载匹配的本地目录，执行版本和选择结果随引擎状态保存。社区规则属于本机 `artifacts`，不提交到公开仓库。
- DAST 候选适配已容忍源码中的 `${PORT}` 等动态 URL 模板，单条非具体 URL 不再让候选接口整体失败。SANDBOX 项目专属 Docker 目标的测试身份引导同时支持常见 HTML 表单与 JSON 注册/登录 API；凭据仍只保存在 API 进程内存，已上线目标不会自动注册。
- DAST 自动策略生成已修复超长 Finding 标题触发数据库 500 的问题：名称在保留用途后缀后限制为 200 字符，失败会回滚并在按钮旁显示原因，重复点击不会重复创建。Semgrep CSRF 中间件缺失已从 XSS 误分类修正为 `csrf-isolated-session`，状态变更方法优先。
- 本地数据库已有 SCA/SAST/AGENT/DAST/SANDBOX/ASPM 联调数据；这些数据是本机状态，不是仓库交付物或通用性能基准。

## 启动顺序

```powershell
docker compose -f infra\docker-compose.yml up -d
python -m alembic -c alembic.ini upgrade head
```

后端：

```powershell
cd apps\api
..\..\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

前端：

```powershell
cd apps\web
npm ci
npm run dev
```

完整配置、扫描流程和测试命令以根目录 [`README.md`](../README.md) 为准。

## 当前能力边界

### SAST

- 已有项目规则、有限语义分析、Git 基线和可选七角色 AI 复核。
- `security_skill` 仍是单次复核输出；知识中枢可以持久化项目候选、人工发布和跨项目推荐，但尚未将知识自动编译成可执行企业 Skill，也没有行业历史漏洞语料或自动改写规则的学习闭环。

### DAST

- 当前候选来自 SAST/AGENT，SCA 不进入队列。
- 源码动态映射主要覆盖 EJS 与 JavaScript/TypeScript 文件族。
- 红/绿裁决必须满足类型专属证据条件；证据不足保持黄色或未验证。

### SANDBOX

- 已有受控启动规划、Docker 隔离、固定探针、事件和证据摘要。
- Node.js 端口发现会跟随 `package.json` 声明/启动脚本及常见 JS/TS 入口；启动前可人工校正端口和健康路径，失败诊断会显示网关实际连接参数。
- 自动辅助服务只覆盖 PostgreSQL/Redis。
- 没有完整文件、网络、进程、环境变量、系统调用监控，也没有 AI 自主攻击。

### ASPM

- 已有单项目总览、证据图、攻击链、整改、复测和报告。
- 已有同租户已发布知识的跨项目匹配推荐；没有项目组、跨项目风险图谱/趋势、SLA、工单、授权配额、完整审计中心或生产合规报告。

### 平台

- 数据库虽有身份/租户模型，但请求链路没有可用的生产身份中间件；大部分接口仍使用开发租户。
- Redis 当前不是核心队列；API/Web 没有生产部署编排。
- 真实密钥、客户源码、离线情报和运行证据不得提交 Git。

## 下一执行顺序

1. P1：可靠任务系统、API/Web 部署工程化，以及版本化安全效果与动态重放语料。
2. P2：IAM/租户权限执行、真实审批身份、角色驱动界面、证据保护、跨项目风险治理、知识采纳效果和企业运维能力。

## 交接纪律

- 规划不能写成现有能力；外部依赖必须标记为有条件实现。
- 不得把未验证、阻塞或黄色解释为安全。
- 不得为准确率、召回率、误报率、DAST 复现率和兼容率填造数据。
- 每次修改都应运行与风险相称的测试、形成独立 Git 提交并推送远端。
- `.tmp/`、`artifacts/`、`outputs/` 和 `.env` 中的本地内容不进入提交。
